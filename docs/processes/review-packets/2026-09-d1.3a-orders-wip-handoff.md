=== ПАКЕТ ДЛЯ CHATGPT (безопасный handoff) ===
Проект: MillQ
Роль получателя: стратегический ревьюер / помощник Product Owner
Задача: оценить статус запуска D1.3A после Accept ADR-0025 и сказать: approve / правки / блокеры / ещё рано

1) Цель блока
- PO явно запустил **D1.3A — Orders Foundation & Consumption Plan** на базе Accepted ADR-0025.
- Нужно: минимальный Orders SoT + RecipeGraphResolver + immutable ConsumptionPlanSnapshot.
- Критическая граница: **никаких** GoodsIssue / InventoryMovement (это D1.3B).
- Autonomy: **Level B** (реализация против Accepted ADR; COMPLETE FULL-DIFF specialist review обязателен).

2) Ссылки
- Branch: `cursor/d1-3a-orders-consumption-plan-9abc`
- Tip commit: **ещё нет commit** (WIP на ветке)
- Base: Origin/GitHub `main` @ `8844ccb` (ADR-0025 Accept / PR #30)
- Canonical baseline от PO: минимум `8844ccb…`; checkpoint PR #17 **не** использовать как base (на main не смержен)
- PR D1.3A: **ещё не создан**
- ADR: `docs/decisions/ADR-0025-order-completion-sale-inventory-write-off.md` — **Accepted / Merged**
- Checkpoint doc: `docs/processes/current-state.md` (пока всё ещё pin «до D1.3A»; не обновлён под WIP)

3) Контекст продукта
- Вертикаль: Sale → Automatic Write-off → Food Cost.
- Sale = экономическое завершение Order (отдельного Sale aggregate нет).
- Write-off trigger = `CompleteOrder` → OrderCompleted; Inventory владеет GoodsIssue (только с D1.3B).
- D1.3A замораживает consumption plan и завершает Order domain-side only.

4) Что сделано в этом блоке (WIP — не готово к merge)
Уже в рабочей копии ветки (не закоммичено / не в PR):
- Миграция `007_orders_consumption_plan.sql`:
  - `outlet.default_sales_issue_warehouse_id`
  - `recipe_specification.sellable_catalog_item_id`
  - `sales_order` / `sales_order_line`
  - `consumption_plan_snapshot` + line / resolved_version / physical_leaf
- Domain: `packages/domain/src/sale-consumption.ts` (+ unit test file)
  - VIRTUAL explode XOR STOCK_TRACKED consume XOR DIRECT_STOCK
  - provenance hash, exactly-one-path assert
- Contracts: fact type `OrderCompleted` + payload schema
- API module skeleton: `apps/api/src/modules/orders/`
  - commands: OpenOrder, Add/Update/Remove line, CancelOrder, CompleteOrder
  - CompleteOrder пишет snapshot + COMPLETED, **без** inventory effects
- Recipes: optional `sellableCatalogItemId` на create draft
- Seed: проставляет default issue warehouse на outlet

5) Что сознательно НЕ сделано / ещё не доведено
- Acceptance tests D1.3A (весь обязательный список PO) — **нет файла**
- Docs: `block-d1.3a-*.md`, update `current-state.md` — **нет**
- Commit / push / Origin PR — **нет**
- Full typecheck/test/build/migrate/health/`git diff --check` на зелёном CI — **не прогонялось до конца**
- Independent COMPLETE FULL-DIFF review — **не запрашивался** (код не готов)
- GoodsIssue / movements / COGS / reversal / Settlement / Payments / Fiscal / POS / KDS / modifiers / Effective Recipe / Food Cost / offline runtime — **OUT** (правильно)

6) Решения
- Accepted (подтверждены, не пересматривались):
  - ADR-0025 (решения PO 1–14)
  - ADR-0002/0003/0008/0009/0010/0016/0018/0019
  - D1.1 / D1.2A / D1.2B границы
- Proposed (нужно решение PO): **нет новых**
- Scaffolding / минимальные config-концепты (в рамках ADR-0025 §11 / binding):
  - `outlet.default_sales_issue_warehouse_id`
  - `recipe_specification.sellable_catalog_item_id` (пока нет Menu module)

7) Проверки
- Тесты: domain unit test файл написан; **acceptance suite не написан / не гонялся**
- Build/typecheck: полный прогон после WIP **не завершён**
- Что не проверялось:
  - все acceptance cases PO (open/add/update/remove/cancel/complete/immutability/resolver/version pin/idempotency/concurrency/warehouse/no inventory)
  - Origin CI
  - backup Origin↔GitHub после merge (merge ещё нет)

8) Риски и вопросы к Product Owner
- D1.3A **не готов к review APPROVE / merge**. Сейчас это промежуточный WIP.
- Среда агента смотрит на GitHub remote; Origin CLI без auth — риск forge-рассинхрона при PR/merge (как на checkpoint #17).
- Приоритет binding: если у sold item есть и recipe binding, и STOCK_TRACKED prep output — сейчас побеждает **recipe explode**. Нужно ли иное правило?
- Нужен ли HTTP surface для Orders в D1.3A или достаточно service + acceptance tests (как D1.1/D1.2)?

9) Рекомендация агента-исполнителя
- **Не merge.** Вердикт для текущего состояния: **BLOCK / ещё рано**.
- Сначала довести: acceptance tests → docs → commit/push → Origin PR (Level B) → COMPLETE FULL-DIFF independent review → green CI → `origin pr merge --auto` → backup → verify SHA.
- После merge D1.3A: **STOP**. D1.3B только по явному PO launch.

10) Просьба к ChatGPT-ревьюеру
Ответь структурировано:
A. Вердикт по **текущему WIP**: BLOCK (ожидаемо) / что обязательно добить до первого formal review
B. Вердикт по **binding-решениям** (`sellable_catalog_item_id`, outlet default warehouse, recipe-vs-STOCK_TRACKED priority): ок / правки
C. Обязательные acceptance cases, без которых merge запрещён
D. Можно ли отдельно добить/смержить только docs checkpoint #17, не мешая D1.3A
E. Что должно войти в ТЗ D1.3B после Accept merge D1.3A

=== КОНЕЦ ПАКЕТА ===
